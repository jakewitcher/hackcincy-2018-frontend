import styled from 'styled-components';

const Wrapper = styled.div.attrs({ className: 'confirmation-page__wrapper' })`
  position: relative;

  height: 100%;

  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
`;

export default Wrapper;
