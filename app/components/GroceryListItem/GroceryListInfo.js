import styled from 'styled-components';
import cssVariables from 'cssVariables';

export const GroceryListInfo = styled.h2`
  font-size: 20px;
  color: ${cssVariables.CLR_FIORD};
  margin: 0 0 8px 24px;
  max-width: 400px;
`;

export default GroceryListInfo;
